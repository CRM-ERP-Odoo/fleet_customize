<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record model="ir.ui.view" id="partner_view_buttons">
        <field name="name">partner.view.buttons</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="groups_id" eval="[(4, ref('fleet.fleet_group_user'))]"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button type="action" class="oe_stat_button" icon="fa-car" name="%(fleet.fleet_vehicle_action)d"
                        context="{'default_driver_id': active_id, 'search_default_driver_id': active_id}">
                    <div class="o_form_field o_stat_info">
                        <span class="o_stat_value">
                            <field name="vehicle_count"/>
                        </span>
                        <span class="o_stat_text">Vehicles</span>
                    </div>
                </button>
            </div>
        </field>
    </record>

</odoo>
